<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.weigend</groupId>
        <artifactId>structure202-parent</artifactId>
        <version>1.0.0</version>
        <relativePath>..</relativePath>
    </parent>

    <artifactId>s202-code-analyzer</artifactId>
    <packaging>jar</packaging>

    <name>S202 Code Analyzer</name>
    <description>JavaFX Tool zur Analyse von Code</description>

    <properties>
        <javafx.version>21.0.5</javafx.version>
        <wfx.version>1.0.1</wfx.version>
        <avaje.version>11.4</avaje.version>
        <slf4j.version>2.0.13</slf4j.version>
        <exec.mainClass>de.weigend.s202.ui.wfx.S202Main</exec.mainClass>
    </properties>

    <dependencies>
        <dependency>
            <groupId>de.weigend</groupId>
            <artifactId>s202-annotations</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- WFX Rich Client Platform modules.
             Keep these explicit so migrated windowmanager modules are not mixed
             with the legacy windowmtg packages still present in older wfx-all jars. -->
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>lookup-api</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>platform-api</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>launcher</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>windowmanager-api</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>ui-utils</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>lookup-avaje</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>platform-core</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>windowmanager-core</artifactId>
            <version>${wfx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.softwareecg.wfx</groupId>
            <artifactId>view-menu</artifactId>
            <version>${wfx.version}</version>
        </dependency>

        <!-- APIs used directly by S202 modules; no longer supplied by wfx-all on the compile classpath. -->
        <dependency>
            <groupId>io.avaje</groupId>
            <artifactId>avaje-inject</artifactId>
            <version>${avaje.version}</version>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>2.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <!-- Logging runtime -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.5.20</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Icons (Material Design pack via Ikonli) -->
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>12.3.1</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-materialdesign2-pack</artifactId>
            <version>12.3.1</version>
        </dependency>

        <!-- JavaFX (pinned to wfx version for the javafx-maven-plugin run goal) -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-web</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- Bytecode Analysis -->
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm</artifactId>
            <version>9.6</version>
        </dependency>
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm-tree</artifactId>
            <version>9.6</version>
        </dependency>
        <dependency>
            <groupId>org.ow2.asm</groupId>
            <artifactId>asm-analysis</artifactId>
            <version>9.6</version>
        </dependency>

        <!-- Project persistence -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.17.2</version>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.10.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.10.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>io.avaje</groupId>
                            <artifactId>avaje-inject-generator</artifactId>
                            <version>${avaje.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>de.weigend.s202.ui.wfx.S202Main</mainClass>
                </configuration>
            </plugin>
            <!-- Classpath-based run used by run-ui.sh (mvn exec:java). Override the main class with
                 -Dexec.mainClass to run the headless tools (CityModelExporter, CitySyncSmoke). -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                </configuration>
            </plugin>
          <!-- generated by avaje inject -->
      <plugin>
        <groupId>io.avaje</groupId>
        <artifactId>avaje-inject-maven-plugin</artifactId>
        <version>11.4</version>
        <executions>
          <execution>
            <?m2e execute?>
            <phase>process-sources</phase>
            <goals>
              <goal>provides</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
    </build>

</project>
