/**
 * The application hexagon: inbound ports (api), outbound ports (spi), and the
 * use-case implementations (service).
 */
@S202Component(displayName = "Application")
package com.paperwhale.application;

import de.weigend.s202.annotation.S202Component;
